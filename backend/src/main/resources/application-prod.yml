spring:
  application:
    name: MailMancer
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://production.host:5432/postgres
    username: service_user_application
    password: service_user_password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

